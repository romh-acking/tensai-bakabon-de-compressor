namespace Libraries
{
    public enum WriteArgs
    {
        action,
        romPath,
        compressedPath,
        dumpPath
    }

    public enum DumpArgs
    {
        action,
        romPath,
        dumpPath
    }
}
